// SKIPPED: requires ../../agents/agent which does not exist in this codebase.
// These tests were written for an agent framework that was never implemented.
describe.skip('Node AI agent framework (stale — module missing)', () => {
  test('placeholder', () => {});
});
