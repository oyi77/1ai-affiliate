#!/usr/bin/env php
<?php

declare(strict_types=1);

/**
 * p1ai - Prosper1ai CLI Client
 *
 * A local CLI tool for managing a remote Prosper1ai installation.
 * Designed for both human operators and AI agents.
 *
 * Quick start:
 *   p1ai config:set-url https://your-Prosper1ai.com
 *   p1ai config:set-key your-api-key-here
 *   p1ai campaign:list
 *
 * All commands support --json for machine-readable output.
 */

// Find autoloader
$autoloadPaths = [
    __DIR__ . '/../vendor/autoload.php',
    __DIR__ . '/../../vendor/autoload.php',
    __DIR__ . '/../../../vendor/autoload.php',
];

$autoloaded = false;
foreach ($autoloadPaths as $path) {
    if (file_exists($path)) {
        require_once $path;
        $autoloaded = true;
        break;
    }
}

if (!$autoloaded) {
    fwrite(STDERR, "Error: Could not find vendor/autoload.php. Run 'composer install' first.\n");
    exit(1);
}

$app = new P202Cli\Application();
$app->run();
